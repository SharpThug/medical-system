<Window x:Class="Client.MainAppWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="МИС ОКО" Height="720" Width="1280">

    <Window.Resources>
        <!-- Стиль для вкладок в стиле браузера -->
        <Style x:Key="BrowserTabStyle" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Name="Root">
                            <Border Name="Border" Background="LightGray" 
                                    BorderBrush="DarkGray" BorderThickness="1,1,1,0"
                                    CornerRadius="6,6,0,0" Margin="2,0,0,0">
                                <ContentPresenter x:Name="ContentSite"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Center"
                                    ContentSource="Header"
                                    Margin="10,5,10,5"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="131*"/>
            <RowDefinition Height="221*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Для кнопки + -->
            <ColumnDefinition Width="*"/>
            <!-- Основное содержимое -->
        </Grid.ColumnDefinitions>

        <!-- Панель вкладок -->
        <TabControl x:Name="MainTabControl" Grid.Column="1"
                    TabStripPlacement="Top"
                    Margin="0,10,0,0"
                    SelectedIndex="0" Grid.RowSpan="2">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource BrowserTabStyle}"/>
            </TabControl.Resources>

            <!-- Вкладка Пациенты -->
            <TabItem Header="Пациенты">
                <Grid Margin="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Заголовок -->
                        <RowDefinition Height="Auto"/>
                        <!-- Период -->
                        <RowDefinition Height="Auto"/>
                        <!-- Имя пациента -->
                        <RowDefinition Height="*"/>
                        <!-- DataGrid + кнопки -->
                    </Grid.RowDefinitions>

                   

                    <Border Grid.Row="1"
        Background="#F0F0F0"
        Padding="10"
        CornerRadius="4">

                        <DockPanel LastChildFill="True">

                            <!-- Кнопки справа -->
                            <StackPanel Orientation="Horizontal"
                    DockPanel.Dock="Right">
                                <Button Content="Расширенный поиск"
                    Margin="0,0,10,0"
                    Padding="15,5"/>

                                <Button Content="Новый пациент"
                    Background="#0078D7"
                    Foreground="White"
                    Padding="15,5"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <DatePicker x:Name="DateFromPicker" 
                    SelectedDateChanged="DateFilter_Changed"
                    Margin="0,0,10,0"
                    Width="120"/>
                                <!-- Фиксированная или минимальная ширина -->

                                <DatePicker x:Name="DateToPicker"
                    SelectedDateChanged="DateFilter_Changed"
                    Margin="0,0,10,0"
                    Width="120"/>
                            </StackPanel>

                        </DockPanel>
                    </Border>


                    <!-- DataGrid + кнопки -->
                    <Grid Grid.Row="3">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <!-- DataGrid растягивается -->
                            <RowDefinition Height="Auto"/>
                            <!-- Кнопки -->
                        </Grid.RowDefinitions>

                        <!-- Таблица пациентов -->
                        <DataGrid x:Name="MainPatientsDataGrid"
                                      Grid.Row="0"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      IsReadOnly="True"
                                      SelectionMode="Single"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Auto"
                                      GridLinesVisibility="All"
                                      BorderBrush="DarkGray"
                                      BorderThickness="1"
                                      Margin="0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Номер карты" Binding="{Binding CardNumber}" Width="120"/>
                                <DataGridTextColumn Header="Запись" Binding="{Binding AppointmentDate}" Width="100"/>
                                <DataGridTextColumn Header="ФИО" Binding="{Binding FullName}" Width="250"/>
                                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate}" Width="120"/>
                                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="150"/>
                                <DataGridTextColumn Header="Пол" Binding="{Binding Gender}" Width="80"/>
                                <DataGridTextColumn Header="Отдел" Binding="{Binding Department}" Width="150"/>
                                <DataGridTextColumn Header="Заболевание" Binding="{Binding Diagnosis}" Width="200"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- Вкладка Записи -->
            <TabItem Header="Записи">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <Grid>
                        <TextBlock Text="Содержимое вкладки Записи" 
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </Grid>
</Window>